- How to get devices?
- Understand EXACT boot processs
- What is qemu doing before it boots the kernel?
- why did I need to turn off stack protection for the UART code?
- mmu / tlb and address translation??
- Do i want a device struct / device table?
- How do i know if Im in "real mode"

- GDT????
- Clean up UART code amd other code too
     -> UART not fully implemented
     -> Magic numbers everywhere
     -> naming conventions
     -> whats funcs are in .h
     -> UART bit shifts
     -> etc
- Add some comment on style + conventions
- Need general IRQ code
- Need specific UART serial ISR's
- Need .config file
- Documetation!!